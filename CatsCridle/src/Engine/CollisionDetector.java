package Engine;

import Objects.Player;

public class CollisionDetector {
   /* private Player player;

    public LevelsObstacles levelsObstacles1 = LevelsObstacles.LEVEL_1;

    public void collided() {
        int currentLevel = 1;
        switch (currentLevel) {
            case 0:
                //------------------MENU------------
                break;
            case 1:
                if(player.getUp() > 0 && levelsObstacles1[player.getUp()][player.getLeft()] = 1){
                    return true;
                }
                break;
            default:
        }


        //return collided(row, col);
    }


    /*public boolean collided(int row, int col) {

        for (GameObject wall : objectList) {
            if (wall instanceof Wall) {
                Wall w = (Wall) wall;
                if (w.getPos().getRow() == row && w.getPos().getCol() == col) {
                    return true;
                }
            }
        }
        return false;
    }
     */

}
